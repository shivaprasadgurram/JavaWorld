package com.designpatterns.singleton;

public enum ENUMDBCon {
    INSTANCE;
}

/**
 * By default, ENUM's are singleton. constructor is private.
 * Everything is automatic, chill and enjoy.
 *
 * One ENUM instance per JVM (one object per JVM)
 */
